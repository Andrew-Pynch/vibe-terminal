```json
{
  "tasks": [
    { "id": "task-1", "description": "List files in the directory", "agent_type": "worker" },
    { "id": "task-2", "description": "List files in the directory", "agent_type": "worker" },
    { "id": "task-3", "description": "List files in the directory", "agent_type": "worker" },
    { "id": "task-4", "description": "List files in the directory", "agent_type": "worker" }
  ]
}
```