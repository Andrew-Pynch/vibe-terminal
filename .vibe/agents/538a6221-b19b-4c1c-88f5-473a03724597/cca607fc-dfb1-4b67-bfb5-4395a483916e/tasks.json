{
  "tasks": [
    { "id": "verify-rate-limit-1", "description": "Start the server and trigger a session to confirm if the 5s delay allows all 4 agents to run without crashing.", "agent_type": "worker" },
    { "id": "multi-provider-investigation-1", "description": "Investigate `claude --help` and other CLI tools to design a generic `ProviderAdapter` trait for the Rust server (abstracting away the specific CLI flags like `-p` vs `--prompt`).", "agent_type": "worker" },
    { "id": "dashboard-phase-7-1", "description": "Once the backend is stable, build the Web UI to visualize these worker logs.", "agent_type": "worker" }
  ]
}
