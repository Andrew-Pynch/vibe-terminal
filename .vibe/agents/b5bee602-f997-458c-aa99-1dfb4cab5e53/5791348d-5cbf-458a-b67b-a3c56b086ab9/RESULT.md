```json
{
  "tasks": [
    { "id": "task-1", "description": "list the files in the directory", "agent_type": "worker" },
    { "id": "task-2", "description": "list the files in the directory", "agent_type": "worker" },
    { "id": "task-3", "description": "list the files in the directory", "agent_type": "worker" },
    { "id": "task-4", "description": "list the files in the directory", "agent_type": "worker" }
  ]
}
```